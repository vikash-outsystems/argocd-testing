apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080 # Change to match your container port
